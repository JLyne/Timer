name: ${project.name}
main: ${project.groupId}.${project.artifactId}.Main
version: ${project.version}
author: LeonTG
description: This plugin adds a timer that will countdown in the action/boss bar of players.
api-version: '1.16'

commands:
  timer:
    description: Timer command, check usage.